// Exercício 1
// Não sabemos o valor do input, mas sabemos que será executado um loop nX, a resposta fica O(n). 

// Exercicio 2
// Não sabemos o valor da string, mas sabemos que será executado um loop nX, a resposta fica O(n). 

// Exercício 
// ficou O(1), porque são executados 7 linhas do código, como é um numero, e não temos loop, então colocamos O(1). 

// Exercício 4
// Como temos um loop, e dentro dele outro loop, por isso ficou  O(n²). 

// Exercício 5
// 3, 1, 2, 4. 

// DESAFIOS

// Exercício 6
// Como o nosso loop depende to tamanho de b. Fica O(b).

// Exercício 7
// Como não tem loop, e o código será executado com o tempo, deacordo com a quantidade de linhas, que é: 4. A resposta é O(1)

// Exercício 8
// O primeiro é O(n²), pois ele executa o loop, e dentro do loop, executa a outra função que também é um loop. 
// O segundo é O(n), pois ele tem um único loop simples. 
